export const CELL_SIZE = 1;
export const CELL_VERTS = new Float32Array([
  -CELL_SIZE,
  -CELL_SIZE,
  CELL_SIZE,
  -CELL_SIZE,
  CELL_SIZE,
  CELL_SIZE,

  -CELL_SIZE,
  -CELL_SIZE,
  CELL_SIZE,
  CELL_SIZE,
  -CELL_SIZE,
  CELL_SIZE,
]);

export const GRID_SIZE = 128;

export const WORKGROUP_SIZE = 8;

export const UPDATE_INTERVAL = 50;
